# Environment Files Priority (first found file is used):
# 1. .env.[NODE_ENV].local (e.g., .env.development.local)
# 2. .env.[NODE_ENV] (e.g., .env.development, .env.production)
# 3. .env.local
# 4. .env (this file)

# Database Configuration
# Local MongoDB with authentication (for development)
MONGO_URI=mongodb://root:example@localhost:27017/shared-media-streaming?authSource=admin

# Alternative: Local MongoDB without auth (uncomment if auth is disabled)
# MONGO_URI=mongodb://localhost:27017/shared-media-streaming

# For MongoDB Atlas (cloud deployment), replace with your connection string:
# MONGO_URI=mongodb+srv://<username>:<password>@<cluster-name>.mongodb.net/shared-media-streaming?retryWrites=true&w=majority

# For Docker Compose MongoDB:
# MONGO_URI=mongodb://root:example@mongo:27017/shared-media-streaming?authSource=admin

# Environment
NODE_ENV=development

# Server Configuration
PORT=3000
